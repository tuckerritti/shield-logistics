{
  "scripts": {
    "setup": "npm install && (cp -n apps/web/env.sample apps/web/.env.local 2>/dev/null || true) && (cp -n apps/engine/env.sample apps/engine/.env.local 2>/dev/null || true)",
    "run": "npm run dev"
  }
}
